{% extends 'base.html.twig' %}

{% block title %}Committee{% endblock %}

{% block body %}
    <h1>Committee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ committee.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ committee.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('committee_index') }}">back to list</a>

    <a href="{{ path('committee_edit', {'id': committee.id}) }}">edit</a>

    {{ include('committee/_delete_form.html.twig') }}
{% endblock %}
