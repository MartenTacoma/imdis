<form method="post" action="{{ path('wg_delete', {'id': wg.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wg.id) }}">
    <button class="btn">Delete</button>
</form>
