{% extends 'base.html.twig' %}

{% block title %}PosterSession{% endblock %}

{% block body %}
    <h1>PosterSession</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ poster_session.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('poster_session_index') }}">back to list</a>

    <a href="{{ path('poster_session_edit', {'id': poster_session.id}) }}">edit</a>

    {{ include('poster_session/_delete_form.html.twig') }}
{% endblock %}
