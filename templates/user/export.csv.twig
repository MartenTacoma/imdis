"Name","email","affiliation","country","registration time","visibility","show_email"{% for event in events %},"{{event.alias}}"{% endfor %}

{% for user in users %}
"{{user.name}}","{{user.email}}","{{user.affiliation}}","{{user.country.name ?? '-'}}","{{user.RegistrationTime|date('Y-m-d H:i:s')}}","{{user.ShowInList}}","{{ user.ShowEmail ? 'show' : 'hide' }}"{% for event in events %},"{% if event in user.event %}Yes{% else %}No{% endif %}"{% endfor %}

{% endfor %}