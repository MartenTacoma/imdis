<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% if app.environment == 'dev' %}[DEV] {% endif %}{{ pagetitle ?? 'Welcome' }}{% if pagetitle is not defined or pagetitle != '' %} - {% endif %}PDF IV - september 2021 - online</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="author" content="Marten Tacoma, NIOZ Royal Netherlands Institute for Sea Research">
        {% block stylesheets %}
            <link rel="stylesheet" type="text/css" href="/style/icomoon/style.css?v20210406" media="screen"/>
            {{ encore_entry_link_tags('app') }}{% endblock %}
        {{ encore_entry_script_tags('app') }}
        {% block javascripts %}{% endblock %}
        {% if app.environment == 'dev' %}<style>#top, #menu {background-color: darkorange}</style>{% endif %}
    </head>
    <body tabIndex=0>
        {% include 'base/blocks/top.twig' %}
        {% include 'base/blocks/menu.twig' %}
        <div id="content">
            <div class="content">
                <h1>{{ pagetitle ?? 'Welcome' }}</h1>
                {#<div class="takedown">This website will go offline early July 2021. All presentations (oral and poster) are and will remain available at the <a href="https://imdis.seadatanet.org">main IMDIS website.</a></div>#}
                {% for message in app.flashes('success') %}
                    <div class="alert alert-success">
                        {{ message }}
                    </div>
                {% endfor %}
                {% block head %}{% endblock %}
        
                {% block body %}
                    <div class="cols reverse">
                        <div class="side">{% block side %}{% endblock %}</div>
                        <div class="main">{% block main %}{{ content }}{% endblock %}</div>
                    </div>
                {% endblock %}
            </div>
        </div>
        {% include 'base/blocks/foot.twig' %}
    </body>
</html>
