{% extends 'base.html.twig' %}

{% block title %}ImdisAbstract{% endblock %}

{% block body %}
    <h1>ImdisAbstract</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ imdis_abstract.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ imdis_abstract.title }}</td>
            </tr>
            <tr>
                <th>ImdisId</th>
                <td>{{ imdis_abstract.imdisId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('imdis_abstract_index') }}">back to list</a>

    <a href="{{ path('imdis_abstract_edit', {'imdisId': imdis_abstract.imdisId}) }}">edit</a>

    {{ include('imdis_abstract/_delete_form.html.twig') }}
{% endblock %}
