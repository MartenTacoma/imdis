<p>Hi {{user.name}},</p>

<p>Somebody (hopefully you) requested a new password for the Polar Data Forum IV account {{user.email}}. If you didn't request a new password you can safely ignore this email.</p>
<p>To reset your password, please visit the following link</p>

<p><a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a></p>

<p>This link will expire in {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

<p>Cheers!</p>
